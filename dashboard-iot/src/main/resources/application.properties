# Conexão com o Supabase (PostgreSQL)
# Substitua o HOST, PORT e NOME_DO_BANCO (geralmente é postgres) pelo link que o Supabase te deu
spring.datasource.url=jdbc:postgresql://yzyawwhrcbiaeyycqcyt.supabase.co:6543/postgres

# Seu usuário e senha do Supabase
spring.datasource.username=postgres
spring.datasource.password=#Claudio(09032008)

# Mantém a conexão viva e mostra o SQL no terminal (bom para debugar)
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect